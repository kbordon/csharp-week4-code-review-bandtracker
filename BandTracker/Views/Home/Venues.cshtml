@{
    Layout = "_Layout";
}
<h1>VENUES</h1>
<hr />
@if (Model.Count != 0)
{
  <ul class="list-unstyled">
    @foreach (var band in Model)
    {
      <li><a href='/venues/@venue.GetId()'>@venue.GetName()</a></li>
    }
  </ul>
}
else
{
    <div class="alert alert-warning">
      <h4><em>There are no venues entered yet.</em></h4>
    </div>
}

<ul class="list-inline">
  <li><a class="other-link" href="/venues/new">ADD A VENUE</a></li>
  <li><a class="other-link" href="/">HOME</a></li>
</ul>
